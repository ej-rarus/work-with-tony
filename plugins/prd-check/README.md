# prd-check

Check a Markdown PRD against your team's template and rule file. Writes `<prd>.check.md` with line numbers, rule names, and fixes. Never edits the PRD.

Full usage is documented at the end of this README once the plugin is complete.
